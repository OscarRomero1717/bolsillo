import { Routes } from '@angular/router';
import { GoalDashboard } from './features/goals/pages/goal-dashboard.component';

export const routes: Routes = [
  { path: '', component: GoalDashboard },
];
